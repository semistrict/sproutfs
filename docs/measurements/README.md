# Measurement reports

The Markdown reports in this directory are versioned. Raw logs, mutation
replays, source snapshots, archives and machine-readable results are retained
locally and ignored by Git.

Links from reports to those raw artifacts work only in a workspace containing
the corresponding evidence. A fresh checkout includes the reports, not the
underlying measurement data.
